{
    "version": 2,
    "framework": "vite",
    "buildCommand": "./node_modules/.bin/vite build",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ]
  }